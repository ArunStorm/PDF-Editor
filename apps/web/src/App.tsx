import Viewer from '@pdf-editor/pdf-ui';
export default function App() { return <Viewer openFile={async () => { const input = document.createElement('input'); input.type = 'file'; input.accept = 'application/pdf'; return new Promise(resolve => { input.onchange = async () => resolve(input.files?.[0] ? input.files[0].arrayBuffer() : undefined); input.click(); }); }} saveFile={async (bytes) => { const blob = new Blob([bytes], { type: 'application/pdf' }); const a = document.createElement('a'); a.href = URL.createObjectURL(blob); a.download = 'edited.pdf'; a.click(); URL.revokeObjectURL(a.href); }} />; }
